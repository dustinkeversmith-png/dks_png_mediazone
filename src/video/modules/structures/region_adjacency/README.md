Half-edge / region adjacency graph (RAG + DCEL).

From a raster label map: faces store area, centroid, mean Lab; half-edges store
twin/next/incident face plus hue ΔE and intensity step across the crack.
