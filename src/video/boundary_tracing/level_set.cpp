Level Set Methods & Chan-Vese (Geometric Active Contours)How it works: Represents the boundary implicitly as the zero-level set of a higher-dimensional function $\phi(x, y) = 0$. The surface evolves according to PDEs driven by image gradients or region statistics (Chan-Vese segmentation doesn't even require clear gradients; it segments by piecewise-constant regions).Pros: Naturally handles splitting, merging, and hole generation without manual re-parameterization. Highly robust to noise and weak boundaries.


Level Set Methods (Implicit Eulerian Formulation):Narrow Band Optimizations: Updating a full signed distance grid $\phi(x, y)$ over the entire image is $\mathcal{O}(N)$ per time step. Modern implementations use a Narrow Band or Fast Marching Method (FMM) ($h \sim 3\text{--}6$ pixels wide around $\phi=0$), dropping iteration cost to $\mathcal{O}(B_{\text{band}})$.Reinitialization Nuance: As $\phi$ evolves under velocity fields, it distorts and loses its Signed Distance Property ($\vert{}\nabla \phi\vert{} = 1$). You must periodically pause and reinitialize $\phi$ by solving:$$\frac{\partial \phi}{\partial t} + \text{sign}(\phi_0)(\vert{}\nabla \phi\vert{} - 1) = 0$$Skipping reinitialization leads to numerical instability and gradient explosions.