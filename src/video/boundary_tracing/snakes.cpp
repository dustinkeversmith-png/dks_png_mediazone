1. Active Contour Models (Deformable Models)Instead of statically tracing a boundary, active contours iteratively evolve a curve to balance internal forces (smoothness, curvature) and external forces (image gradients, intensity edges).A. Snakes (Parametric Active Contours - Kass et al.)How it works: A spline initializes near the object and minimizes an energy functional:$$E = \int \left( \alpha \vert{}v'(s)\vert{}^2 + \beta \vert{}v''(s)\vert{}^2 + E_{\text{ext}}(v(s)) \right) ds$$$\alpha$ controls tension (elasticity, prevents stretching).$\beta$ controls rigidity (prevents sharp bends/corners).$E_{\text{ext}}$ pulls the contour toward high-gradient edges.Pros: Produces naturally smooth, physically plausible vector curves directly from raw images.Cons: Cannot naturally split or merge if there are multiple objects or holes (topological changes break parameterization).


    Parametric Snakes vs. Geometric Level SetsParametric Snakes (Explicit Lagrangian Formulation):Nuance: Discretized into $M$ points $v_1, \dots, v_M$. Solving the Euler-Lagrange equations requires inverting a cyclic pentadiagonal matrix:$$(A + \gamma I) V^{(t)} = \gamma V^{(t-1)} - \nabla E_{\text{ext}}(V^{(t-1)})$$Inversion takes $\mathcal{O}(M)$ per iteration via the Thomas algorithm adapted for cyclic boundaries.Failure Mode: Boundary Concavities. Traditional snake forces cannot push the contour into deep U-shaped bays or inlets. To fix this, you must switch to Gradient Vector Flow (GVF) fields, which requires solving an isotropic diffusion PDE over the entire image grid prior to tracing ($\mathcal{O}(K_{\text{diff}} \cdot N)$).