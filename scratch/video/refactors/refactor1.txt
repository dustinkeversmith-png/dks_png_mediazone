NEW ATOMIC STRUCTURE
 no preprocessing images and storing in data, instead the raw rgbs are always passed to the atoms and then the atoms them selves do all the preprocessing steps expected at different stateges dictated by the atom.config.json file in each

? grab the accurate preproccesing stage or expected stage for each atom, update the config descriptors, and ensure the preprocessing happens inside of the atomic unit tests

? implement all of the dataset providers and their cooresponding provider desc with adequate renderings

? implement dataset provider atoms for in dataset/atoms/subfolder as cpp and for each output the specific renderings as artifacts and ground truth locations

?
also set preferred dataset and update all atoms to use the new providers

MOVED ARTIFACTS INSIDE RELATIVE FOLDER OF THE ATOM TEST
 no longer with they be stored haphazardly randomly in the external artifacts of the atoms folder and additionally their config will be set as well as 
 
 atom.artifact.json determining the outputs expected on a run

? update each atomic unit config with comprehensive output expectations, all stage outputs

? update the atomic unit tests to output to the correct coorepsonding artifacts folder nearest in the directory of the atom

DATASETS UPGRADE

 NO LONGER PREPROCESSING OR STORING PREPROCESSED IMAGES OF THE DATASETS
 
 ADDED ade20k_subset_500mb and SBD
 
 MOVED ALL DATSET FOLDERS TO THE EXTERNAL VISION FOLDER RATHER THAN BENCHMARKS FOLDER WAS CONFUSING


? integrate the new datasets into their ideal unit tests based on the annotations they provide etc

DATASET DESCRIPTORS

 each dataset folder gets a root descriptor file now explaining what is in the dataset, what kind of ground truths it provides etc.
 
 # FOR EACH DATSET create the dataset.desc and provider.desc.json

 
 
 DATASET ATOMIC TESTS
 
  added a new layer to independently provide the datasets, render out puts etc and atomic tests for each dataset implemented as a provider to verify the ground truths, renderings etc.
  
  
# DATASET script locations

 moved the loose fetching scripts and removed the preprocessing scripts and atomic preparation into a scripts folder
 
# DATASET atoms folder

 removed the atoms folder of preprocessed images which is a no no
 

# Created the dataset provider folder in /video where all the dataset provider classes will go.

 for each dataset create a dataset provider, providing the dataset
 
# I REMOVED ALL INTEGRATION TESTS WE ARENT READY FOR THAT AND IT GETS IN THE WAY.


# I ALSO REMOVED ALL THE BENCHMARKS BECAUSE WE ARENT READY FOR THAT JUST YET UNTIL I DEFINE THE SCHEMA

# I MOVED ANY COORESPONDING PARSERS SUCH AS COCO_PARSER INTO THE COORESPONDING datasets provider sub folder.


? hook up the parser specifics into the provider and any independent logic necessary



# I DECOUPLED SOME OF THE METRICS.hpp which included some domain specific functionality, also eventually seperating the geometry or math into its own sub module

? hook these back up into where previously the monolithic metrics and other image operations were specificed

I also removed the includes themselves from the metrics.hpp we need to update the includes of the cooresponding module to include it from the closest helpers directory.

METRICS in general needs to be organized itself.

I also organized general domain specific functions into cooresponding domain folders in the hopes of getting rid of the contour_kit folder entirely

as well as moved io or vision_io into datasets/io, in some cases some datasets might need their own specialized io functionality.. so this is the work around for now. 

I also moved distance_transform which was dangling in math into sdf/felzen.hpp since its related to distance transformes

I moved image_out into datasets/io as well to sepeerate this from a core module but a external set, some dataset providers may need to use specialized image_io anyways

I moved the examples of the dependencies into the /dependencies fodler

# FURTHUR CLEARING OUT METRICS

cleared the rest out of here boundary pixels has something to do with image operations, so I created a new module sub folder /image as always if you can seperate domain specifics into subfolders, and then extract the domain specific functions such as boundary_pixels : -> image/boundary

tightness_ration :-> image/masks

boundary_f1 :-> image/boundary



collect_on :-> image/geometry



mean_min_distance needs to be moved into the math domain, hausdorff as well,



and the score functions moved into this new folder I made in datasets called datasets/evaluation

# TRY TO PREVENT COUPLING LIKE THIS IN THE FUTURE TO PREVENT ALL OF THIS REFACTORING.

# AND FINALLY I DELTED CONTOUR_KIT
since it really isn't a modular feature specific thing but instead a shared monolithic header which I would rather have repeated functions in namespaces 