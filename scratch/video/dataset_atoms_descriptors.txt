



Verify Dataset Types

 COCO 
  (coarse metrics)
  .jpg
  anotations
   bbox
   segmentation
   category_id
  Parallel Renderings
   segementations, and bbox
 LVIS
  annotations : bbox, segementation
  
 DISK5
  just raw images and their raw masks for foreground and background removal matting
  
 SBD (Semantic Boundaries Dataset)
  .mat format
  GTcls (semantic classes) GTinst (for object instances)
  .Segmentation 2D Integer Matrix of Class IDs
  .Boundaries Cell Array mapping boundaries per class
  
 ADEKE20K
  jpg every day images
  groundtruths:
   _seg.png semantic mask (RGB) red channel category id, green channel instance id
   
   .json (Rich Metadata) polygon shapes, bounding boxes, physical relationships
    .polygon
	box
	attributes.
 
 BSDS500
  .mat file
  groundTruth{i}.Segementation 2D integer matrix [Height, width] i-th humans annoator region segmentation. Each connection region/super pixel
  groundTruth.Boundaries 2D binary matrix (uint8) 1 represents edge/contour pixel traced regions
  
 
  
  
   
  Supposed to provide (polygon vertices, bounding boxes, occlusion status"





Needed Atoms
 Hue Gradient Delta E Map
	Color / Hue: Difference in CIE-Lab space across local horizontal/vertical stencils ($\Delta E_{ab}$).
 Crease / Valley filter
  Morphological Black Top-Hat transform or Laplacian of Gaussian ($I - \text{close}(I)$) to catch dark grooves.
 SLIC / Felzenszwalb Grid Graph
  Treat each pixel as a node. Edge weights between neighbors are set by your combined metric $E(x, y)$.In a single sorted pass over edges, merge pixels into regions using a Disjoint-Set (Union-Find) data structure based on an internal variation threshold.Why this fixes your problem:Every single pixel is guaranteed to belong to a closed region.No open polylines, no dangling line segments, and no unclosed gaps.
 
 Half-Edge / Region Adjacency Graph
 Distance Transform (EDT) + Watershed
 
 Planar Straight-Line Graph (PSLG) with a Doubly-Connected Edge List (DCEL / Half-Edge structure)
 
 
 
Verify Current Atoms:
 Moore Neighbor Only Detects External Hard Course No Internal Holes, Need advanced multi centroid moore
 
 bbox_auto
 just calculate bounding box of one singular object in with the correct background filtered.
 
 bilateral_filter
 not really sure its doing anything.
 
 morph
 not really sure its doing anything.
 
 
 
 snakes
 calculates similar to moore neighbor for contour or edge but 
 
 chamfer
 sdf style
 
 dual contouring
 accurate contour external contour
 
 ccl
 screen boxes like a good bounding boxes.
 
 gvf
 edges filter to jpg
 
 unit_laplace_response
 outer edge light inner edge dark, everything else gray
 
 unit_laplace_zero_Cross
 has like more edge and kind of lip features.
 
 unit_level_set
 dont really know how this works
 
 live_wire
 outer contour
 
 marching squares
 contouring with sometimes inside geometry finding
 
 unit_templace_ncc
 pretty much broken
 
 unit_sobel
 some kind of edge and orientation
 
 rtree, rdp dont really have good outputs
 
UPGRADE_INPUTS:
 Marching Squares / Grayscale Intensity, Multi-thresholdSDF Run across multiple isovalues / gradient thresholds (e.g., MSER / multi-level thresholding), or on an internal edge energy field.

 output the normal gradient field for DUAL contouring
 construct sdf containing interior zero-crossing, or subtracting from the main hull?
 
 LIVEWIRE
  RAW RGB / Grayscale Input | Seed points intelligent around the internal sub features?
  
 Graph CUT
  RAW RGB + Gaussian Mixture Models | Formulate as a multi label Potts model
  
 Snakes
  Raw Image Gradient
  (GVF)
  initialize multiple independent splines seeded inside the region, driven by internal gradient extrema
  
  Level Sets
   Partial Differential Equations on Image Intensity / Edge Map
   Use Mumford-Shah functional (Chan-verse) or multi phase level sets 2^N capture topolgical splits
  
  Moore Neighbor
   input Binary Grid
   use Suzuki-Abe
   
  Bilateral
   sigma_r might be to high or to low 3-5 or 0.1-, 0.3 intensity
  Morphology
   Kernel Size might be too small
  
  SDF for internals Chamfer
   Internal edges, compute generalized distance transform on the edge strength map (Canny/Sobel) not filled silloute
   
   
  Template NCC
   need actual templates to compare





Distance Transform + Watershed
Multi-Label Alpha-Expansion GraphCut

Recursive Bounding or Internal and External Edge Expansion

GraphCut / GrabCut doesn't really demonstrat what it does.

Main bottle neck is rotation data, texturing etc